version: '3.7'

services:
  web:
    deploy:
      mode: global
    depends_on:
      - server
    networks:
      - app
    image: nginx:1.23
    ports:
      - 80:80
    volumes:
      - ../nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ../nginx/default.html:/usr/share/nginx/html/index.html

  server:
    image: 127.0.0.1:5000/ts-app:0.0.1
    command: npm run start
    deploy:
      replicas: 3
      update_config:
        parallelism: 1
        order: start-first
        failure_action: rollback
        delay: 10s
      rollback_config:
        parallelism: 0
        order: stop-first
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 5
        window: 120s

  mysqldb:
    volumes:
      - mysql_prod:/var/lib/mysql

volumes:
  mysql_prod:
    driver: local

networks:
  app:
    driver: overlay
